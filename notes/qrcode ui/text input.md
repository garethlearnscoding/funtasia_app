# Text Input handling

## Considering Basement and Ground level markers 
The input shld be numeric text input which can take 2 digits - text input which can take 2 digits, remove the "M" and "L" from the input.